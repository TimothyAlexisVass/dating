<div class="user-profile">
  <h2><%= link_to "#{user.first_name} #{user.last_name} (#{user.age})", user_path(user.username) %></h2>
  <%= image_tag "#{user.gender}.svg" %>
  <%= ct(:birth_date) %>
  <%= user.birth_date %>
  <%= ct(:rebirth_date) %>
  <%= user.rebirth_date %>
</div>
